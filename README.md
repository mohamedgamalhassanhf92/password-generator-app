# password-generator-app
A simple Python password generator that creates strong, random passwords using letters, digits, and symbols. The script allows generating secure passwords with a default length of 12 characters.
